# Triton Kernel 优化失败记录

## 问题

动态配置调优 (`_get_kernel_config`) 导致性能下降 **6-10%**。

## 数据

| 版本 | 平均执行时间 |
|------|-------------|
| v1 (优化前) | ~1.13 ms |
| v2 (优化后) | ~1.22 ms |

## 原因

1. `head_dim=512` 时 `num_warps=8` 增加了 warp 调度开销
2. `num_stages=3` 增加了 shared memory 和流水线延迟
3. TOPK=2048 较大时，更多 stages 反而增加开销

## 解决方案

恢复固定配置 `num_warps=4, num_stages=2`
